<tr>
  <td><%= question.id %></td>
  <td><%= question.body %></td>
  <td><%= link_to t('form.show'), admin_question_path(question) %></td>
  <td><%= link_to t('form.edit'), edit_admin_question_path(question) %></td>
  <td>
    <%= link_to t('form.destroy'), admin_question_path(question), method: :delete, data: { confirm: t('.you_sure') } %>
  </td>
</tr>