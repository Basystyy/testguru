<h1><%= answer_header(@question) %></h1>

<%= form_with model: [@question, @answer], local: true do |form| %>
  <%= render partial: 'form', locals: {form: form}%>
<% end %>


<p>
  <%= link_to 'Back', admin_question_path(@question) %>
</p>
