<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to t('form.show'), admin_test_path(test)%></td>
  <td><%= link_to t('form.edit'), edit_admin_test_path(test)  %></td>
  <td>
    <%= link_to t('form.destroy'), admin_test_path(test), method: :delete, data: { confirm: t('form.you_sure') } %>
  </td>
</tr>