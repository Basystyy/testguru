<h1><%= t('.header') %></h1>

<%= link_to t('.new'), new_user_registration_path %><br>

<%= link_to t('.login'), new_user_session_path %><br>

<p>
  <%= link_to t('.new_test'), new_admin_test_path %>
</p>

<table>
  <tr>
    <th><%= t('.title') %></th>
    <th><%= t('.level') %></th>
    <th><%= t('.question') %></th>
    <th colspan="3"><%= t('.actions') %></th>
  </tr>

  <%= render @tests %>
</table>
