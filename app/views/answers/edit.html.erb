<h1><%= answer_header(@answer.question) %> </h1>

<%= form_with model: @answer, local: true do |form| %>
  <%= render partial: 'form', locals: {form: form}%>
<% end %>

<p>
  <%= link_to 'Back', question_path(@answer.question) %>
</p>
